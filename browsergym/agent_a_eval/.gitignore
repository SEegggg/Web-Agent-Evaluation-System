# Chrome browser profile (cookies, cache, login data, sessions)
browser_profile/

# Auth state (contains tokens/cookies)
auth_state.json

# Experiment results
results/
reports/
logs/

# Local config overrides (keep template clean)
config.local.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
